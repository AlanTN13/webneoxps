name: NexOps Engineering Dispatcher

on:
  issues:
    types:
      - opened

jobs:
  smoke:
    if: github.actor == 'AlanTN13'
    runs-on: ubuntu-latest
    steps:
      - name: Confirm issue trigger
        run: echo "Engineering issue #${{ github.event.issue.number }} received"
